describe("ProductCatalogue", () => {
  test.todo("should allow products to be added to the catalogue");

  test.todo("should allow products to be removed from the catalogue");

  test.todo("should throw an error if an out of stock product is removed");

  test.todo(
    "should return a list of products, including their prices and quantities"
  );
});
